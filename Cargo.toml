[package]
name = "mbox-to-text"
version = "0.1.0"
edition = "2021"

[dependencies]
mailparse = "0.15"
encoding_rs = "0.8"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
